server:
  port: 8081

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: fixes
          uri: https://github.com/xaawii/car-registry-microservices
          search-paths: config-data