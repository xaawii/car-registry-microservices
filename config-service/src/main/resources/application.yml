server:
  port: 8081

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: brand-service
          uri: https://github.com/xaawii/car-registry-microservices
          search-paths: config-data